import { Canvas, Meta } from '@storybook/blocks';

import Button from './Button.stories';

<Meta of={Button} />

# Button Documentation

In this section you will learn more about button component. Button is an atom in our design system. You can use button as follows.

```javascript
<Button title='Demo Button' onClick={() => {}}> Click Me </Button>
```